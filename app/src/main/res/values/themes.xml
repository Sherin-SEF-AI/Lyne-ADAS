<resources>
    <!-- Compose drives all UI; this XML theme only needs to be a valid no-action-bar base
         with a matte-black window so the camera surface has no flash-of-white at launch. -->
    <style name="Theme.LyneADAS" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:navigationBarColor">@android:color/black</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
    </style>
</resources>
